# 停车位管理系统 · 执行路线图

## 用户决策（2026-09-08）
- **尺寸**：双尺寸并行
  - PC 后台：**1440 × 900**（在 1920×1080 内的紧凑布局，便于表格密度控制；工具包 width 上限 10000 允许）
  - 移动端：**390 × 844**
- **范围**：**8 页全部交付**
- **交互策略**：**静态外观 + 显式标注**（按钮/文字占位 + 红字"仅外观"角标 + `conversion-report.json` warnings 字段记录所有缺失行为）

## 全局设计 token（写进每个 project.json）

### 主色（PC 商务蓝 / 移动端商务蓝）
| 用途 | 色值 |
|---|---|
| 主色 | `#2563eb` |
| 主色-hover | `#1d4ed8` |
| 主色-light | `#dbeafe` |

### 状态色
| 状态 | 色值 |
|---|---|
| 成功/空闲 | `#10b981` |
| 警告 | `#f59e0b` |
| 危险/告警/欠费 | `#ef4444` |
| 信息/占用 | `#3b82f6` |
| 中性/停用 | `#9ca3af` |

### 文字/背景
| 用途 | 色值 |
|---|---|
| 主文字 | `#111827` |
| 次文字 | `#6b7280` |
| 弱文字 | `#9ca3af` |
| 背景 | `#f5f6f8` |
| 卡片 | `#ffffff` |
| 边框 | `#e5e7eb` |

### 深色台（仅出入口监控页用）
| 用途 | 色值 |
|---|---|
| 深色背景 | `#0f172a` |
| 深色面板 | `#1e293b` |
| 深色文字 | `#e2e8f0` |
| 深色边框 | `#334155` |

### 字体
- 主：`Microsoft YaHei` / `微软雅黑`（目标机需安装，Windows 默认有）
- 备：`Arial, sans-serif`

### 圆角/边框
- 卡片：`radius: 8`
- 按钮：`radius: 6`
- 表格：`radius: 4`
- 输入框：`radius: 6`，边框 1px `#d1d5db`

## 8 个页面目录与节奏

| 序号 | 目录 | 尺寸 | 复杂度 | 备注 |
|---|---|---|---|---|
| **P01** | `P01-PC-出入监控/` | 1440×900 | ★★★★★ | 深色台 + 视频 + 告警 + 按钮集群；最复杂，**先做这个**验证 |
| P02 | `P02-PC-车位资源/` | 1440×900 | ★★★ | 表格为主 |
| P03 | `P03-PC-收费计费/` | 1440×900 | ★★★★ | 参数卡 + 表格 |
| P04 | `P04-PC-权限档案/` | 1440×900 | ★★★★ | 树形 + 表格 + 日志 |
| P05 | `P05-PC-基础报表/` | 1440×900 | ★★★ | 统计卡 + 表格 |
| M01 | `M01-移动-首页/` | 390×844 | ★★ | 状态卡 + 通知 |
| M02 | `M02-移动-停车服务/` | 390×844 | ★★★ | 列表 + 缴费占位 |
| M03 | `M03-移动-个人中心/` | 390×844 | ★★ | 车牌管理 + 月卡 |

## 每页交付物
```
<PAGE_DIR>/
├── project.json           ← 单一事实来源
├── assets/                ← 图片（如无则空）
└── (构建后)
versions/
└── vN-<PAGE>-.../
    ├── index.html         ← 网页预览
    ├── project.json       ← CLI 复制的副本
    └── assets/...         ← CLI 复制的资源
```

## 单页迭代节奏
1. 更新 `<PAGE_DIR>/project.json`（新增/修改节点）
2. `node <TOOLKIT>/converter/cli.cjs check <project.json>`（先过校验）
3. `node <TOOLKIT>/converter/cli.cjs web <project.json> versions/vN-<PAGE>-描述`
4. 给用户看网页
5. 用户提修改 → 回到 1

## RP 最终交付节奏（用户说"定稿，生成 RP"后）
- 8 个 project.json 全部 check 过
- `node <TOOLKIT>/converter/cli.cjs rp <project.json> versions/vFinal-<PAGE>` 逐页跑
- 检查每个 conversion-report.json
- 给出 Axure RP 9 验收清单（每个 rp 都打开 → 验证页面名/字段/颜色/单选 → 重新发布 HTML 看资源完整性）

## 不支持项统一标注规范

每个交互位（按钮/弹窗/搜索框等）使用：
- 类型用 `button`/`text`
- 视觉上：右侧或下方加一个红色边框小标签 `text "仅外观 无行为"`，`fill:"#fff1f0"` `color:"#ef4444"` `fontSize:10`
- 视频流/抓拍图：用 `rect` 深色填充 + 居中 `text` 标注（如"实时视频监控画面 占位"）
- conversion-report.json 的 warnings 字段由 CLI 自动写入工具包已知限制，我们手动维护一份 `MISSING_FEATURES.md` 列每个页面的具体缺口

## 当前进度
- ✅ 2026-09-08 起步：工具包阅读 / 环境验证 / 项目骨架 / 需求文档 / 路线图
- ⏳ P01 出入口监控 v1（下一步）
- ⏸ P02–P05、M01–M03 排队