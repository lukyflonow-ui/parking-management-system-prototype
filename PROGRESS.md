# 停车位管理系统 · 进度记录

> 本文件用于跨会话延续工作。请每次重要节点后更新。

## 项目位置
- 项目根目录：`/Users/meixiaoxue/Developer/10-html转rp/00-停车位管理系统`
- 工具包（共享依赖，**不要修改**）：`/Users/meixiaoxue/Developer/10-html转rp/HTML-RP-Toolkit-0.1.0`
- 屏幕规格：**手机 390×844**

## 工作约定（按用户指令）
- 工具包作为只读依赖，不在工具包内写入任何东西。
- 每次修改必须 **先改 project.json + assets**，再用 CLI 生成网页与 RP；不允许只改 HTML 后声称 RP 已同步。
- 输出目录每次新建（CLI 不覆盖），保留到 `versions/`：`v1-xxx`、`v2-xxx` ……
- 网页和 RP 都以 project.json 为单一事实来源。
- 直到用户明确说"**定稿，生成 RP**"，才执行 `rp` 或 `build` 命令。
- 当前轮不生成 RP，只做网页预览。
- 工具包不支持的控件或交互必须显式标注，不静默省略。

## 工具包 0.1.0 硬限制（重读 CAPABILITIES.md 后的摘要，避免重复犯错）
- 单页 ≤2000 节点；只能产 1 个 Axure 页面，**没有页面跳转/母版/动态面板**。
- 类型：`rect / text / button(仅外观) / image(PNG/JPEG) / radio(互斥) / group`。
- 文字：单段纯文字，无 HTML、无富文本、无换行；垂直居中、无内边距。
- 颜色：`#RRGGBB`；字号 6–200；圆角 0–短边一半（圆=短边一半）；边框 0–20。
- 图片：项目内 `assets/` 下 PNG/JPEG，单图 <20MB，**不支持 SVG/裁切/圆角**。
- button：可写外观，但 actions 必须空数组；不携带点击/弹窗/筛选/搜索/表单提交行为。
- radio：每个 group 至多 1 个默认选中；原生互斥。
- group：只声明层级、不绘制自身背景；背景请单独放 rect 子节点。
- 字体：fontFamily 需在目标机（验收用 Windows + Axure RP 9）已安装。
- 不支持：透明度、阴影、自动布局、React/JS、条件渲染、筛选、弹窗、连接线、母版、动态面板。

## 用户最新指令（2026-09-08 反馈后）
- **整体风格改浅色**（推翻原"深色监控台"思路）
- **PC 尺寸改 1920×1080**（移动端仍 390×844，M-overview 大图横向并列展示）
- **简约美观**
- **记录表扩为多列**
- **左侧大功能菜单列全**
- **小程序单独一个网页**（一张大图横向展示 3 个 Tab 概览）
- **这一轮只做 P01 v2 + M-overview v1**，P02–P05 等用户验收后再继续
- **（新）每个菜单都要有独立的页面** → 折中：每个菜单 = 独立 project.json + 独立 .rp 文件，多轮产出
- **（新）Element UI 控件样式做成设计规范** → 已落地 `DESIGN-TOKENS.md`，所有页面复用

## 关于"每个菜单独立页面"的折中方案
- 工具包 0.1.0 硬限制：**单页项目**；每次 `rp` 命令只能从 1 个 project.json 产出 1 个 .rp 文件
- 不能用"页面跳转"在同一 .rp 内跨菜单
- **采用方案**：每张菜单 = 1 个独立目录（P01-PC-出入监控 / P02-PC-车位资源 / ...）+ 1 个 project.json + 1 个 .rp 文件，最终交付 13 个 .rp 文件
- 每张 RP 内容完整（左侧菜单 + 顶部 + 主区 + 控件规范预览区 + 底部标注），用户可在 Axure RP 9 中按文件名打开对应页面

## 当前状态
- 2026-09-08：环境检查通过（Node v22.22.2 / doctor PASS / selftest PASS / 项目骨架就绪）。
- 目录结构：
  - `assets/` 空，待放图片。
  - `versions/` 保存每一轮 web/rp 输出，保留所有历史版本。
  - `requirements.md` 完整需求已落地（PC 5 页 + 移动 3 页 = 8 个独立页面）。
  - `ROADMAP.md` 执行路线图：设计 token、页面节奏、迭代流程已定。
  - `DESIGN-TOKENS.md` **已建立**：Element UI 完整规范（色彩/排版/间距/圆角/11 类组件/标注规范）
- 已确认：双尺寸并行（PC 1920×1080 / 移动 390×844）、8 页全做、交互策略"静态外观+显式标注"。
- ✅ P01 出入口监控 v1 完成（91 节点，深色台 1440×900）— **已废弃**
- ✅ P01 出入口监控 v2 完成（184 节点，浅色 1920×1080，13 项 4 段菜单）— **已废弃**
- ✅ P01 出入口监控 v3 完成（299 节点，Element UI + 控件规范预览区）— **已废弃**
- ✅ P01 出入口监控 v4 完成（314 节点，业务版无规范预览区）— **已废弃**（侧栏 fill 透传 bug）
- ✅ P01 出入口监控 v5 完成（287 节点，fill 修复版）— **已废弃**
- ✅ **P01 出入口监控 v6 完成（394 节点，菜单升级 + 业务版）** — `versions/v6-P01-出入监控-菜单升级/`
  - 4 段命名规范对称：实时监控 / 车位管理 / 收费中心 / 系统管理（每段 4 项）
  - 项命名清晰统一：出入监控 / 出入流水 / 现场视频 / 异常告警 / 车位总览 / 车位地图 / 预约管理 / 黑名单 / 订单管理 / 收费规则 / 月卡管理 / 发票管理 / 组织架构 / 角色权限 / 操作日志 / 数据字典（16 项，满铺 4 段 × 4 项）
  - 视觉升级：logo 区独立二级底 + 段标题加粗 + 每项 4×4 圆角彩色图标占位（蓝/橙/绿/红/灰 5 色轮换）+ 激活态主色满铺 + 左侧 3px 主色描边条 + "当前"徽章 + 非激活显示页码 + 菜单项间细分隔线
  - 底部 footer：版权 + 技术支持
  - 行高 48px、字号 14、所有节点 fill 字段透传正确
- ✅ M-overview 移动端概览 v1 完成（112 节点，浅色 1920×1080）— `versions/v1-M-overview-移动端概览/`
- ⏸ P02/P03/P04/P05 等用户验收 P01 v6 后继续，沿用 v6 的菜单 + T(fill) 模式
- ⏸ M01/M02/M03 详情页：等用户决定是否需要

## 目录结构（最新）
```
/Users/meixiaoxue/Developer/10-html转rp/
├── DESIGN-TOKENS.md           ← Element UI 设计规范（项目外，所有业务系统复用）
├── HTML-RP-Toolkit-0.1.0/     ← 工具包（只读）
└── 00-停车位管理系统/
    ├── PROJECT.json 等项目源
    ├── P01-PC-出入监控/project.json
    ├── P02-PC-车位资源/...
    ├── M-overview-移动端概览/project.json
    ├── requirements.md
    ├── ROADMAP.md
    ├── PROGRESS.md
    └── versions/
        ├── v1-P01-出入监控/                 ← 已废弃
        ├── v2-P01-出入监控-浅色/             ← 已废弃
        ├── v3-P01-出入监控-Element规范/      ← 已废弃
        ├── v4-P01-出入监控-业务版/           ← 已废弃
        ├── v5-P01-出入监控-fill修复/         ← 当前
        └── v1-M-overview-移动端概览/         ← 当前
```

## 待用户补充（开放问题）
- [ ] P01 v2 验收是否通过？要调整哪里？
- [ ] M-overview 的三屏是否覆盖你想要的全部小程序功能？还是需要继续做 M01/M02/M03 单屏详情？
- [ ] P02 车位资源 的第一屏内容是否要先约定（先看板还是先表格）？
- [ ] 是否需要 logo、品牌主色？目前默认 `#2563eb` 作为主色。

## 待办（按步骤）
1. 等用户提交需求 → 整理功能清单 → 按单页一屏多段 group 思路写 v1 `project.json`。
2. `node <TOOLkit>/converter/cli.cjs web <project.json> versions/v1-xxx` → 预览网页 → 让用户验收。
3. 迭代：每次只改 `project.json` + `assets/`，再重生成网页。
4. 用户说"定稿，生成 RP" → 重新跑最新 project.json 过 `check` → `rp` 或 `build` → 检查 conversion-report.json → 给出 Axure RP 9 验收步骤。